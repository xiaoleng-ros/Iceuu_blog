我将通过以下步骤为您实现独立的分类页面功能：

1.  **创建分类页面模板 (`app/category/[category]/page.tsx`)**
    *   新建动态路由页面，用于处理“生活边角料”、“情绪随笔”等分类请求。
    *   **布局复刻**：完全复用首页的 `PublicLayout` 和 `Hero` 组件，确保页头、页脚、Banner 背景与首页一致。
    *   **数据获取**：编写服务端数据获取逻辑，通过 Supabase 筛选指定分类的文章数据。
    *   **内容渲染**：使用首页同款的 `PostList` 组件展示文章列表。
    *   **模块移除**：在页面结构中**不引入** `Sidebar` 组件，从而彻底移除“个人信息（图二）”、“站点运行（图三）”和“学无止境（图四）”这三个模块。
    *   **布局调整**：保持主内容区域的容器结构，确保文章列表在无侧边栏的情况下自然展示（或根据视觉效果保持居中）。

2.  **更新导航栏链接 (`components/layout/Header.tsx`)**
    *   修改顶部的导航配置数组 `navItems`。
    *   将原有的 `/blog?category=...` 查询参数形式链接，更新为新的路由路径 `/category/...`。
    *   确保中文分类名称在 URL 中被正确处理。

3.  **验证与优化**
    *   确认点击导航栏链接能正确跳转到对应分类页。
    *   验证分类页仅显示该分类下的文章。
    *   检查页面视觉风格是否与首页保持高度一致（除已移除模块外）。
